{
    "name": "task-tracker-monorepo",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "dev": "pnpm -r --parallel dev",
        "build": "pnpm -r build",
        "start": "pnpm --filter backend start"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}